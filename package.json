{
  "name": "ctci",
  "version": "1.0.0",
  "description": "doing some ctci exercises",
  "main": "index.js",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
